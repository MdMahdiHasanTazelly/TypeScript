
const fruit: string[] = ["Apple", "mango", "banana"];

const price: Array<number> = [3, 2, 1];

type Student = {
    name: string;
    isActive: boolean;
}

const allStudents: Student[] = [];

allStudents.push({name: "Mahdi", isActive: true});

//2D array

let matrix: number[][] = [ [], [] ];